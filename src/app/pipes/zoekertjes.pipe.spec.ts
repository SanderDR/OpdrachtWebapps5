import { ZoekertjesPipe } from './zoekertjes.pipe';

describe('ZoekertjesPipe', () => {
  it('create an instance', () => {
    const pipe = new ZoekertjesPipe();
    expect(pipe).toBeTruthy();
  });
});
